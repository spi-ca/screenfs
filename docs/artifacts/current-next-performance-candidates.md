# Current next performance candidates

이 문서는 `70a3105` (`docs(perf): 성능 후속 근거와 검증 정리`) 체크포인트 이후의 all-five-lane post-metadata follow-up 상태에서, 체크인된 artifact가 **무엇을 입증하는지 / 무엇을 아직 입증하지 못하는지 / 다음 실험이 무엇인지**를 한곳에 정리한 evidence snapshot이다. `920c00e` 또는 dirty-worktree provenance를 가진 linked summary는 current baseline이 아니라 historical rejected/coverage context로만 읽는다. historical five lanes은 `open_confined` / `openat2`, `readdirplus` page/scan, matcher-heavy policy path, read/write follow-up beyond the completed fallback small-I/O baseline, mutation invalidation breadth-first다. Formal gate와 scope label 정의는 [`../benchmarks.md`](../benchmarks.md)의 `Current next-priority candidate gates`가 source of truth다. refreshed next-step ordering은 direct opened-object `stat_child_no_follow()` path under `open_confined`가 [`open-confined-stat-child-open-path/summary.md`](open-confined-stat-child-open-path/summary.md)에서 kept claim-grade evidence로 승격되고, fast-policy/cache-eligible `readdirplus` page-scan fast path가 [`readdirplus-page-scan-claim/summary.md`](readdirplus-page-scan-claim/summary.md)에서 kept claim-grade evidence로 승격되고, matcher-heavy no-writable mutability fast path가 [`matcher-no-writable-mutability-fast-path/summary.md`](matcher-no-writable-mutability-fast-path/summary.md)에서 rejected evidence로 남은 현재 기준으로, immediate next unfinished work인 matcher-heavy policy path, 이후 read/write follow-up beyond the completed fallback small-I/O baseline, mutation invalidation breadth-first다. metadata/open-path umbrella의 `lookup`/`getattr` same-path no-follow attr reuse는 현재 코드에 이미 들어간 implemented slice로서 [`metadata-open-path-claim/attr-reuse-worktree-19ca971/summary.md`](metadata-open-path-claim/attr-reuse-worktree-19ca971/summary.md) dirty-worktree slice evidence와 함께 읽고, 다음 미완료 implementation pick으로 다시 제시하지 않는다. 아래 matrix는 historical five-lane artifact coverage를 유지한다. 현재 evidence 기준 immediate next unfinished pick은 matcher-heavy policy path row다. regenerated fallback smokes는 여전히 attribution baseline이고, rejected [`open-confined-frequency/summary.md`](open-confined-frequency/summary.md) / [`open-confined-fd-path/summary.md`](open-confined-fd-path/summary.md) bundles는 rejected alternatives로 남는다; 이제 `open_confined` lane의 kept speedup evidence는 새 direct opened-object claim artifact가 맡는다.

## Evidence matrix

| candidate | current label | current artifacts | what current artifacts prove | what they do **not** prove | next experiment / gap | rejected evidence |
| --- | --- | --- | --- | --- | --- | --- |
| `readdirplus` page/scan | `claim` | [`post-metadata-follow-up-claim/worktree-3cb95ba/summary.md`](post-metadata-follow-up-claim/worktree-3cb95ba/summary.md), [`readdirplus-deferred-attrs/summary.md`](readdirplus-deferred-attrs/summary.md), [`readdirplus-20k-policy-ablation/summary.md`](readdirplus-20k-policy-ablation/summary.md), [`current-readdirplus-page-scan-attribution-smoke/summary.md`](current-readdirplus-page-scan-attribution-smoke/summary.md), [`readdirplus-visibility-batch-proof/summary.md`](readdirplus-visibility-batch-proof/summary.md), [`directory-child-visibility-parent-local-series/worktree-8720eb8d82ba/summary.md`](directory-child-visibility-parent-local-series/worktree-8720eb8d82ba/summary.md), [`readdirplus-page-scan-claim/summary.md`](readdirplus-page-scan-claim/summary.md), [`current-directory-symlink-surface-smoke.md`](current-directory-symlink-surface-smoke.md), rejected [`readdirplus-bounded-candidate-heap/summary.md`](readdirplus-bounded-candidate-heap/summary.md), rejected [`readdirplus-returned-policy-recheck/summary.md`](readdirplus-returned-policy-recheck/summary.md) | current directory claim rows keep non-zero `readdirplus_directory_scan`, `readdirplus_attr_generation_scan`, and `readdirplus_page_commit`; deferred-attrs bundle proves attr-work reduction for non-returned entries and a 5k same-machine improvement; the current 5k/20k fallback-vs-fast ablation is dirty-worktree coverage whose provenance includes regenerated `current-open-confined-surface-smoke*` / `current-metadata-opendir-smoke*`, `scripts/bench-screenfs.py`, `src/fs.rs`, `src/fs/guards.rs`, `src/fs/perf.rs`, `src/fs/tests/perf.rs`, so it is not an isolated no-code-change control, but it still shows the focused 20k row is directory-scan-dominant and policy-sensitive; the current page-scan attribution smoke further splits that cost and shows `readdirplus_scan.scan_visibility` as the largest visible sub-bucket in the sampled 5k/20k rows while `scan_fallback_attr` is zero on this filesystem; the current fast-policy claim bundle proves the conservative all-visible batch fast path clears the fast `directory-surface` and focused 20k `readdirplus_basic` gates | a broad all-policy `readdirplus` latency win; a matcher-heavy descendant claim; a symlink-visibility speedup claim; any before/after speedup from this ablation; any kept `readdirplus` optimization claim from this ablation; or page/scan retuning that survives both directory-surface `readdir_basic` non-regression and focused 20k `readdirplus_basic` p50/p95/p99 latency gates | current kept fast-policy scope complete; future broader policy/matcher or symlink-visible claims need fresh same-matrix before/after rows, and symlink logic changes still require `directory-symlink-surface`; the current parent-local `DirectoryChildVisibilityBatch` / DP-like `scan_visibility` slice is `readdirplus`-only and limited to default-hidden/no-hidden/no-internal-hidden visible-subtree-anchor policy shapes; [`directory-child-visibility-parent-local-series/worktree-8720eb8d82ba/summary.md`](directory-child-visibility-parent-local-series/worktree-8720eb8d82ba/summary.md) is coverage-only/no-claim evidence for that slice; broader policy/matcher expansion or speed claims must use matcher-descendant plus repeated/interleaved evidence rather than the fast-policy claim gate alone; [`readdirplus-visibility-classification-feasibility.md`](readdirplus-visibility-classification-feasibility.md) records why broad directory-local prefix classification is risky; [`readdirplus-visibility-batch-proof/summary.md`](readdirplus-visibility-batch-proof/summary.md) records the conservative proof scope | [`readdirplus-page-scan-path-join/summary.md`](readdirplus-page-scan-path-join/summary.md) is rejected page/scan retuning (5k `readdir_basic` p50 `1.557941x`, 20k `readdirplus_basic` p50 `1.036241x`, code reverted), [`readdirplus-bounded-candidate-heap/summary.md`](readdirplus-bounded-candidate-heap/summary.md) is rejected bounded candidate-selection evidence (20k `readdirplus_basic` p50 improved to `0.882012x` but p95/p99 missed at `0.998063x`/`1.002933x`, fast `directory-surface` `readdirplus_basic` regressed `1.062710x`, matcher32 `readdir_basic` regressed `1.480425x`, code reverted), [`readdirplus-returned-policy-recheck/summary.md`](readdirplus-returned-policy-recheck/summary.md) is rejected returned-entry policy-recheck evidence (fast `readdir_basic` p50 `1.121x`, fast `readdirplus_basic` p50/p95/p99 `0.951x`/`0.994x`/`0.994x`, matcher32 `readdirplus_basic` p50 `1.242x`, focused 20k p50 `1.010x`, code reverted), and [`directory-symlink-resolver-reuse/summary.md`](directory-symlink-resolver-reuse/summary.md) is rejected symlink-directory work, not kept speedup evidence |
| read/write follow-up beyond fallback small-I/O baseline | `smoke` | [`read-write-small-io-guard-reuse/summary.md`](read-write-small-io-guard-reuse/summary.md), [`read-write-small-io-guard-reuse/validation.log`](read-write-small-io-guard-reuse/validation.log), coverage-only [`read-write-followup-current/summary.md`](read-write-followup-current/summary.md), coverage-only [`read-write-storage-backed/summary.md`](read-write-storage-backed/summary.md), coverage-only [`read-write-cold-cache-approx/summary.md`](read-write-cold-cache-approx/summary.md), rejected [`read-write-fast-snapshot-path/summary.md`](read-write-fast-snapshot-path/summary.md) | the checked-in bundle provides a completed fallback small-I/O claim baseline, and the companion validation log records the maintained local validation checks; the current follow-up bundle adds fast/fallback default `read-write-surface` rows, alternate same-default-`64MiB` sequential-size rows for a reduced small/random mix, and separate `256MiB` larger-sequential coverage rows. The storage-backed bundle adds local `/home/...` btrfs coverage for both `read-write-surface` and `read-write-concurrency` / `concurrent_rand_read_write_4k` with `--concurrency-workers 4`. The cold-cache-approx bundle adds local btrfs non-root read-side `--cache-control posix-fadvise-read-fixture` coverage for fast/fallback surface + concurrency rows; its JSON-backed stats record method `posix-fadvise-read-fixture`, `42` applications on each surface row, `14` on each concurrency row, read-side-only backing-source fixture files, and mounted samples evicting backing source paths rather than mounted aliases. The split-counter interpretation remains consistent: fast-path guard cost stays near zero on warm-cache default (`16ns` / `16ns`), 256MiB (`13ns` / `14ns`), storage-backed surface (`17ns` / `19ns`), and storage-backed concurrency (`21ns` / `20ns`) rows, while fallback stays guard-dominant on default (`18105ns` / `24486ns`), storage-backed surface (`24827ns` / `31047ns`), and storage-backed concurrency (`34768ns` / `46630ns`) rows | a separate fast-policy read/write claim; a larger-sequential win; any before/after claim from the current coverage rows; or a strict literal cold-cache claim. The rejected fast snapshot-path attempt does not prove a kept fast-policy claim because only 2 of 4 targeted small-I/O rows cleared the full p50/p95/p99 improvement gate, `seq_write` missed non-regression, and fallback context regressed. The approximation bundle does not reset write-side, dentry, inode, or device caches | keep the checked-in fallback bundle as baseline/context, keep the current warm-cache/storage-backed/concurrency/approximation bundles as coverage-only evidence, then add dedicated before/after rows for whichever speedup lane is claimed. For this post-`70a3105` retry, choose exactly one read/write sub-axis before implementation and pin its matrix/gate: fast-policy small-I/O uses the existing `read-write-surface` 3-of-4 small-I/O gate; larger-sequential claims require `seq_read` and/or `seq_write` in matching `256MiB` rows at `<= 0.90x` median and `<= 0.95x` p95/p99 while untouched small/random rows stay within non-regression; storage-backed claims require the same row-specific threshold on local btrfs rows with matching storage provenance; concurrency claims require `concurrent_rand_read_write_4k` at `<= 0.90x` median and `<= 0.95x` p95/p99 with matching `--concurrency-workers`; cold-cache-sensitive approximation claims require matching `--cache-control posix-fadvise-read-fixture` read-side rows and may only claim approximation movement, not literal cold cache. `read-write-cold-cache-approx` is the non-root approximation answer for that earlier run; the current fallback concurrency same-binary series was collected after `/dev/fuse` became available and remains control/no-claim evidence because pair 3 has a large tail outlier (see [`read-write-concurrency-series-control/worktree-8720eb8/summary.md`](read-write-concurrency-series-control/worktree-8720eb8/summary.md)), while strict literal cold-cache remains an explicit non-claim/scope boundary and privileged cache-drop runs are not part of this harness | [`read-write-fast-snapshot-path/summary.md`](read-write-fast-snapshot-path/summary.md) is rejected fast-policy snapshot-path evidence: path-clone removal was behavior-safe in focused tests/reviews, but the claim gate failed (`small_read`/`small_write` passed, `rand_read_4k` and `rand_write_4k` missed, `seq_write` median `1.054102x`, fallback small rows regressed), so the Rust implementation was reverted/not kept. |
| mutation invalidation breadth-first | `smoke` | [`current-mutation-invalidation-set-smoke.md`](current-mutation-invalidation-set-smoke.md), [`current-mutation-invalidation-smoke.md`](current-mutation-invalidation-smoke.md), [`current-pinned-mutation-invalidation-smoke.md`](current-pinned-mutation-invalidation-smoke.md), rejected [`mutation-invalidation-prefix-range/summary.md`](mutation-invalidation-prefix-range/summary.md), focused correctness/tests described in [`../performance-roadmap.md`](../performance-roadmap.md) | mounted plumbing now reports `invalidations.scanned_entries`; pinned smoke exercises listing-adjacent invalidation paths; focused tests prove sibling/non-descendant mappings survive and invalidated inodes evict after `FORGET` in focused state coverage | a kept mounted latency win; a breadth-first `invalidations.scanned_entries` reduction that survives the latency gate; mounted post-`FORGET` observability as claim evidence unless a dedicated driver exists. The prefix-range retry reduced scan breadth but did not survive the latency gate. | refresh a focused current-shape `subtree_rename_cached_unrelated` baseline first, then prove `invalidations.scanned_entries` reduction on the targeted row while clearing the latency gate; add a dedicated post-`FORGET` mounted driver only if the claim includes eviction breadth, otherwise document the limitation/rejected/coverage artifact explicitly | [`mutation-invalidation-range-scan/summary.md`](mutation-invalidation-range-scan/summary.md) is a rejected ordered-subtree scan experiment and stale/rejected evidence for the current workload scope, [`mutation-invalidation-breadth-index/summary.md`](mutation-invalidation-breadth-index/summary.md) is rejected/slice-context current-shape evidence: `invalidations.scanned_entries` dropped (`15392 -> 572` set, `10764 -> 52` focused) but the latency gate failed (`symlink_parent_mkdir_rmdir` p50 `1.239281x`, `pinned_symlink_parent_mkdir_rmdir` p50 `1.295362x`, set `subtree_rename_cached_unrelated` p50 `1.439480x`, focused subtree p50 `1.027902x`), so the child-index Rust implementation was reverted/not kept. [`mutation-invalidation-prefix-range/summary.md`](mutation-invalidation-prefix-range/summary.md) is rejected current-shape prefix-range evidence: scans dropped (`15392 -> 884` set, `10764 -> 104` focused), but the documented full-set gate failed because all three set workloads regressed (`symlink_parent_mkdir_rmdir` p50 `1.170160x`, `pinned_symlink_parent_mkdir_rmdir` p50 `1.343853x`, set `subtree_rename_cached_unrelated` p50 `1.222235x`); the focused row is supporting context only (`0.960478x` p50, improved tails), so the Rust implementation was reverted/not kept. |
| `open_confined` / `openat2` frequency | `claim` | [`open-confined-stat-child-open-path/summary.md`](open-confined-stat-child-open-path/summary.md), regenerated [`current-open-confined-surface-smoke.md`](current-open-confined-surface-smoke.md), regenerated [`current-metadata-opendir-smoke.md`](current-metadata-opendir-smoke.md), context-only [`metadata-open-path-claim/guard-context-worktree-19ca971/summary.md`](metadata-open-path-claim/guard-context-worktree-19ca971/summary.md) | the checked-in summary provides a kept claim-grade `open-confined-surface` speedup for the direct opened-object `stat_child_no_follow()` path: fallback `metadata_open` `0.559x`/`0.532x`/`0.516x`, fallback `metadata_opendir` `0.597x`/`0.567x`/`0.553x`, fast `metadata_open` `0.505x`/`0.596x`/`0.598x`, and fast `metadata_opendir` `0.646x`/`0.558x`/`0.562x` at p50/p95/p99. Companion fallback metadata context rows stay within non-regression, and the counter split shows `stat_child_no_follow` time moving from parent-dirfd revalidation/`fstatat()` to direct opened-object `host_fstat` without changing `open_confined_openat2` confinement. The regenerated smokes remain surrounding-cost attribution context. | a blanket metadata/open-path win; proof that `openat2` itself, rather than surrounding policy/path work, is the dominant remaining cost; permission to skip opened-fd revalidation, hidden `ENOENT`, symlink target visibility, or validation; or proof that the regenerated smokes are speedup evidence by themselves | the next unfinished lane is now matcher-heavy policy path; if future `open_confined` work resumes, reuse the same fallback/fast `open-confined-surface` matrix and add explicit `metadata_opendir` only as opendir-specific attribution/non-regression context | [`open-confined-frequency/summary.md`](open-confined-frequency/summary.md) is rejected/slice-context evidence: the focused fallback `metadata_opendir` row improved (`0.658506x` p50) and reduced resolver/open-confined counters, but the final fallback `open-confined-surface` row regressed (`metadata_open` p50 `1.341903x`, `metadata_opendir` p50 `1.152927x`) and the fast row kept tail regressions, so the implementation was reverted/not kept. [`open-confined-fd-path/summary.md`](open-confined-fd-path/summary.md) is also rejected evidence: the allocation-focused `/proc/self/fd/<fd>` builder preserved guardrails in local validation/review, but fallback `open-confined-surface` regressed (`metadata_open` p50 `1.630311x`, `metadata_opendir` p50 `1.419060x`), fast tails missed non-regression, and focused `metadata_opendir` regressed, so the Rust implementation was reverted/not kept. |
| matcher-heavy policy path | `smoke` | [`current-policy-heavy-matrix-smoke.md`](current-policy-heavy-matrix-smoke.md), [`current-matcher-descendant-directory-smoke.md`](current-matcher-descendant-directory-smoke.md), [`current-matcher-source-attribution-smoke/summary.md`](current-matcher-source-attribution-smoke/summary.md), [`current-matcher-mutability-source-attribution-smoke/summary.md`](current-matcher-mutability-source-attribution-smoke/summary.md), [`matcher-heavy-no-code-control/summary.md`](matcher-heavy-no-code-control/summary.md), [`matcher-heavy-descendant-no-code-control/summary.md`](matcher-heavy-descendant-no-code-control/summary.md), [`matcher-heavy-series-control/worktree-8720eb8/summary.md`](matcher-heavy-series-control/worktree-8720eb8/summary.md), rejected [`matcher-no-writable-mutability-fast-path/summary.md`](matcher-no-writable-mutability-fast-path/summary.md), rejected [`matcher-rank-zero-early-stop/summary.md`](matcher-rank-zero-early-stop/summary.md), rejected [`matcher-subtree-only-best-candidate/summary.md`](matcher-subtree-only-best-candidate/summary.md), rejected [`matcher-visible-default-short-circuit/summary.md`](matcher-visible-default-short-circuit/summary.md), rejected [`matcher-empty-family-lookup-skip/summary.md`](matcher-empty-family-lookup-skip/summary.md), context-only [`metadata-open-path-claim/summary.md`](metadata-open-path-claim/summary.md) and [`metadata-open-path-claim/guard-context-worktree-19ca971/summary.md`](metadata-open-path-claim/guard-context-worktree-19ca971/summary.md) | current matcher32 smoke remains metadata-heavy context that proves subtree-family candidate volume and non-zero path/descendant candidate-order counters; source-attribution instrumentation supports `internal_hidden.path`, `hidden.path`, `visible.path`, `visible.descendant`, `readonly.path`, and `writable.path`, while the current refreshed policy-heavy smoke observes both visibility-side labels (`internal_hidden.path`, `hidden.path`, `visible.path`, `visible.descendant`) and mutability-side readonly/writable source counters via `matcher_readonly_access_wok`; the separate mutability-source smoke keeps focused read-only-close and explicit readonly-access attribution for the same source labels; dedicated matcher-descendant smoke adds non-empty visible descendant workload coverage for `matcher_descendant_readdir` / `matcher_descendant_readdirplus` and keeps non-zero `matcher_candidate_order.descendant`, `readdir_directory_scan`, `readdirplus_directory_scan`, `readdirplus_attr_generation_scan`, `readdirplus_candidate_selection`, and `readdirplus_page_commit` counters | a before/after matcher-descendant improvement; a broad visible-descendant speedup; a claim-grade speedup on `matcher_descendant_readdir` / `matcher_descendant_readdirplus`; duplicate-removal cost; rank-0 early-stop as a kept path-side win; broader directory-hot or symlink-visibility claims; or any descendant claim that still relies only on empty visible-matcher counters or one-way smoke timing | no kept matcher-heavy speedup yet; future matcher work still needs a same-surface before/after `--workload-set matcher-descendant-directory` pair for `matcher_descendant_readdir` / `matcher_descendant_readdirplus`, or focused trace/microbenchmark/counter evidence if aggregate counters stay ambiguous | [`matcher-descendant-combined-probe/summary.md`](matcher-descendant-combined-probe/summary.md) is rejected combined-probe evidence: `metadata_lookup` / `metadata_access` stayed near-neutral-to-regressive, `matcher_descendant_readdir` improved only marginally and missed p99 non-regression, `matcher_descendant_readdirplus` regressed, the state gate failed, and the Rust optimization was reverted/not kept. [`matcher-rank-zero-early-stop/summary.md`](matcher-rank-zero-early-stop/summary.md) is rejected path-side evidence: winner semantics were preserved, but `policy-heavy-matrix` missed the improvement gate (`matcher_hidden_stat_miss` p50 `1.008929x`, metadata rows neutral/regressive) and companion `matcher-descendant-directory` regressed badly (`matcher_descendant_readdir` p50 `1.314779x`, `matcher_descendant_readdirplus` p50 `1.469457x`), so the Rust implementation was reverted/not kept. [`matcher-subtree-only-best-candidate/summary.md`](matcher-subtree-only-best-candidate/summary.md) is rejected subtree-only path-side evidence: focused candidate-order equivalence tests passed, but `policy-heavy-matrix` matcher32 regressed (`matcher_hidden_stat_miss` p50/p95/p99 `1.177x`/`1.352x`/`1.352x`, metadata p50 `1.216x`-`1.368x`), so the implementation was not kept. [`matcher-visible-default-short-circuit/summary.md`](matcher-visible-default-short-circuit/summary.md) is rejected visible-default short-circuit evidence: semantics tests passed and `policy_decision.total_ns` decreased in one run, but mounted policy-heavy matcher32 rows regressed, so the implementation was not kept. |

## Honest-scope reminders

- [`readdirplus-page-scan-claim/summary.md`](readdirplus-page-scan-claim/summary.md) is the kept fast-policy/cache-eligible page-scan claim for the conservative directory child visibility batch fast path; it is not a broad all-policy or matcher-heavy claim.
- `readdirplus-deferred-attrs` is an attr-work reduction slice, not a blanket all-policy `readdirplus` speedup claim across directory sizes.
- [`readdirplus-20k-policy-ablation/summary.md`](readdirplus-20k-policy-ablation/summary.md) is current dirty-worktree coverage only: the companion provenance records `git_worktree_clean=False` / `screenfs_source_git_worktree_clean=False`, includes regenerated open-like attribution docs plus `scripts/bench-screenfs.py`, `src/fs.rs`, `src/fs/guards.rs`, `src/fs/perf.rs`, `src/fs/tests/perf.rs`, and separates 5k/20k plus fallback/fast policy effects better than the rejected path-join bundle. It is not a before/after speedup claim, not a kept `readdirplus` optimization claim, and not an isolated no-code-change control.
- `readdirplus-page-scan-path-join` is rejected evidence only: some focused 20k counters/tails improved, but the implementation was reverted after 5k `readdir_basic` regressed badly and 20k `readdirplus_basic` p50 still regressed. [`readdirplus-returned-policy-recheck/summary.md`](readdirplus-returned-policy-recheck/summary.md) is also rejected evidence only: the returned-entry policy recheck skip was reverted after fast `readdir_basic` regressed, fast `readdirplus_basic` missed the gate, matcher32 `readdirplus_basic` p50 regressed, and focused 20k p50 regressed.
- `read-write-small-io-guard-reuse` is a fallback-policy claim bundle kept here as baseline/context. [`read-write-followup-current/summary.md`](read-write-followup-current/summary.md), [`read-write-storage-backed/summary.md`](read-write-storage-backed/summary.md), and [`read-write-cold-cache-approx/summary.md`](read-write-cold-cache-approx/summary.md) now add current fast/fallback default rows, alternate same-`64MiB` reduced small/random mix rows, separate `256MiB` larger-sequential rows, local btrfs storage-backed/concurrency coverage, and local btrfs non-root read-side `posix-fadvise-read-fixture` approximation coverage. They are still current dirty-worktree local coverage only: not before/after claims, not separate fast-policy claims, and not strict literal cold-cache evidence.
- mutation invalidation currently has observability and focused correctness coverage, not a kept speedup artifact; the current lane is breadth-first, the previous ordered-range scan bundle is rejected/stale for the current workload scope, the newer breadth-index bundle is also rejected/slice-context only because scanned-entry wins did not clear the latency gate, and post-`FORGET` observability must be added separately or explicitly documented as limitation/rejected/coverage artifact.
- `open_confined` now has a kept claim-grade bundle at [`open-confined-stat-child-open-path/summary.md`](open-confined-stat-child-open-path/summary.md) for the direct opened-object `stat_child_no_follow()` path. Regenerated [`current-open-confined-surface-smoke.md`](current-open-confined-surface-smoke.md) and [`current-metadata-opendir-smoke.md`](current-metadata-opendir-smoke.md) still add surrounding-cost split coverage only; they do not broaden that claim into a blanket metadata/open-path win or override the rejected [`open-confined-frequency/summary.md`](open-confined-frequency/summary.md) / [`open-confined-fd-path/summary.md`](open-confined-fd-path/summary.md) bundles.
- `lookup`/`getattr` same-path no-follow attr reuse는 현재 코드의 `src/fs/guards.rs` `reply_entry_for_path()` / `attr_for_path()` / `guard_read_path_with_resolver(..., Some(&attr))` known-attr flow와 `src/fs/tests/perf.rs` / `src/fs/tests/perf/**` (`perf_counters_record_lookup_getattr_stat_reuse`, `perf_counters_record_missing_lookup_stat_failure_reuse`) focused coverage로 확인되는 implemented slice다. Dedicated slice evidence는 [`metadata-open-path-claim/attr-reuse-worktree-19ca971/summary.md`](metadata-open-path-claim/attr-reuse-worktree-19ca971/summary.md)에 있지만, 이 bundle은 dirty-worktree before/after pair이므로 slice-scoped current-worktree evidence일 뿐 full metadata/open-path candidate claim은 아니다.
- matcher-heavy policy evidence now includes rejected no-writable mutability fast-path evidence at [`matcher-no-writable-mutability-fast-path/summary.md`](matcher-no-writable-mutability-fast-path/summary.md), plus metadata-heavy context and dedicated descendant smoke/coverage. [`current-policy-heavy-matrix-smoke.md`](current-policy-heavy-matrix-smoke.md) keeps the matcher32 metadata row, and [`current-matcher-descendant-directory-smoke.md`](current-matcher-descendant-directory-smoke.md) proves non-empty visible descendant workload coverage with non-zero descendant/directory/readdirplus counters. [`matcher-descendant-combined-probe/summary.md`](matcher-descendant-combined-probe/summary.md), [`matcher-subtree-only-best-candidate/summary.md`](matcher-subtree-only-best-candidate/summary.md), and [`matcher-visible-default-short-circuit/summary.md`](matcher-visible-default-short-circuit/summary.md) remain rejected implementation evidence only.
