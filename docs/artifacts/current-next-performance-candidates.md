# Current next performance candidates

이 문서는 all-five-lane post-metadata follow-up run 이후, 체크인된 artifact가 **무엇을 입증하는지 / 무엇을 아직 입증하지 못하는지 / 다음 실험이 무엇인지**를 한곳에 정리한 evidence snapshot이다. 다섯 lane은 `readdirplus` page/scan, read/write follow-up beyond the completed fallback small-I/O baseline, mutation invalidation breadth-first, `open_confined` / `openat2`, matcher-heavy policy path다. Formal gate와 scope label 정의는 [`../benchmarks.md`](../benchmarks.md)의 `Current next-priority candidate gates`가 source of truth다. 현재 evidence 기준 immediate next measure-first pick은 `open_confined` / `openat2` frequency row다. 이유는 regenerated fallback smokes가 이제 `open_like.pre_open_guard.*` / `open_like.post_open_revalidation.*` surrounding-cost split coverage까지 보여주더라도 여전히 attribution baseline일 뿐이고, rejected [`open-confined-frequency/summary.md`](open-confined-frequency/summary.md) bundle도 focused `metadata_opendir` improvement/counter reduction은 보여줬지만 claim-gate `open-confined-surface` rows를 통과하지 못해 kept speedup evidence가 아니기 때문이다.

## Evidence matrix

| candidate | current label | current artifacts | what current artifacts prove | what they do **not** prove | next experiment / gap | rejected evidence |
| --- | --- | --- | --- | --- | --- | --- |
| `readdirplus` page/scan | `slice` | [`post-metadata-follow-up-claim/worktree-3cb95ba/summary.md`](post-metadata-follow-up-claim/worktree-3cb95ba/summary.md), [`readdirplus-deferred-attrs/summary.md`](readdirplus-deferred-attrs/summary.md), [`readdirplus-20k-policy-ablation/summary.md`](readdirplus-20k-policy-ablation/summary.md), [`current-directory-symlink-surface-smoke.md`](current-directory-symlink-surface-smoke.md) | current directory claim rows keep non-zero `readdirplus_directory_scan`, `readdirplus_attr_generation_scan`, and `readdirplus_page_commit`; deferred-attrs bundle proves attr-work reduction for non-returned entries and a 5k same-machine improvement; the current 5k/20k fallback-vs-fast ablation is dirty-worktree coverage whose provenance includes regenerated `current-open-confined-surface-smoke*` / `current-metadata-opendir-smoke*`, `scripts/bench-screenfs.py`, `src/fs.rs`, `src/fs/guards.rs`, `src/fs/perf.rs`, `src/fs/tests/perf.rs`, so it is not an isolated no-code-change control, but it still shows the focused 20k row is directory-scan-dominant and policy-sensitive | a broad `readdirplus` latency win across directory sizes; a matcher-heavy descendant claim; a symlink-visibility speedup claim; any before/after speedup from this ablation; any kept `readdirplus` optimization claim from this ablation; or page/scan retuning that survives both 5k `readdir_basic` and focused 20k `readdirplus_basic` latency gates | claim-grade `directory-surface` before/after rows plus a focused large-directory `readdirplus_basic` row with page/scan counters; add `directory-symlink-surface` when symlink logic changes | [`readdirplus-page-scan-path-join/summary.md`](readdirplus-page-scan-path-join/summary.md) is rejected page/scan retuning (5k `readdir_basic` p50 `1.557941x`, 20k `readdirplus_basic` p50 `1.036241x`, code reverted), and [`directory-symlink-resolver-reuse/summary.md`](directory-symlink-resolver-reuse/summary.md) is rejected symlink-directory work, not kept speedup evidence |
| read/write follow-up beyond fallback small-I/O baseline | `smoke` | [`read-write-small-io-guard-reuse/summary.md`](read-write-small-io-guard-reuse/summary.md), [`read-write-small-io-guard-reuse/validation.log`](read-write-small-io-guard-reuse/validation.log), coverage-only [`read-write-followup-current/summary.md`](read-write-followup-current/summary.md), coverage-only [`read-write-storage-backed/summary.md`](read-write-storage-backed/summary.md), coverage-only [`read-write-cold-cache-approx/summary.md`](read-write-cold-cache-approx/summary.md) | the checked-in bundle provides a completed fallback small-I/O claim baseline, and the companion validation log records the maintained local validation checks; the current follow-up bundle adds fast/fallback default `read-write-surface` rows, alternate same-default-`64MiB` sequential-size rows for a reduced small/random mix, and separate `256MiB` larger-sequential coverage rows. The storage-backed bundle adds local `/home/...` btrfs coverage for both `read-write-surface` and `read-write-concurrency` / `concurrent_rand_read_write_4k` with `--concurrency-workers 4`. The cold-cache-approx bundle adds local btrfs non-root read-side `--cache-control posix-fadvise-read-fixture` coverage for fast/fallback surface + concurrency rows; its JSON-backed stats record method `posix-fadvise-read-fixture`, `42` applications on each surface row, `14` on each concurrency row, read-side-only backing-source fixture files, and mounted samples evicting backing source paths rather than mounted aliases. The split-counter interpretation remains consistent: fast-path guard cost stays near zero on warm-cache default (`16ns` / `16ns`), 256MiB (`13ns` / `14ns`), storage-backed surface (`17ns` / `19ns`), and storage-backed concurrency (`21ns` / `20ns`) rows, while fallback stays guard-dominant on default (`18105ns` / `24486ns`), storage-backed surface (`24827ns` / `31047ns`), and storage-backed concurrency (`34768ns` / `46630ns`) rows | a separate fast-policy read/write claim; a larger-sequential win; any before/after claim from these current rows; or a strict literal cold-cache claim. The approximation bundle does not reset write-side, dentry, inode, or device caches | keep the checked-in fallback bundle as baseline/context, keep the current warm-cache/storage-backed/concurrency/approximation bundles as coverage-only evidence, then add dedicated before/after rows for whichever speedup lane is claimed. `read-write-cold-cache-approx` is the non-root approximation answer for this run; no environment blocker remains for this lane, while strict literal cold-cache remains an explicit non-claim/scope boundary and privileged cache-drop runs are not part of this harness | — |
| mutation invalidation breadth-first | `smoke` | [`current-mutation-invalidation-set-smoke.md`](current-mutation-invalidation-set-smoke.md), [`current-mutation-invalidation-smoke.md`](current-mutation-invalidation-smoke.md), [`current-pinned-mutation-invalidation-smoke.md`](current-pinned-mutation-invalidation-smoke.md), focused correctness/tests described in [`../performance-roadmap.md`](../performance-roadmap.md) | mounted plumbing now reports `invalidations.scanned_entries`; pinned smoke exercises listing-adjacent invalidation paths; focused tests prove sibling/non-descendant mappings survive and invalidated inodes evict after `FORGET` in focused state coverage | a kept mounted latency win; a fresh focused subtree baseline on the current workload shape; a breadth-first `invalidations.scanned_entries` reduction that survives the latency gate; mounted post-`FORGET` observability as claim evidence unless a dedicated driver exists | refresh a focused current-shape `subtree_rename_cached_unrelated` baseline first, then prove `invalidations.scanned_entries` reduction on the targeted row while clearing the latency gate; add a dedicated post-`FORGET` mounted driver only if the claim includes eviction breadth, otherwise document the limitation/rejected/coverage artifact explicitly | [`mutation-invalidation-range-scan/summary.md`](mutation-invalidation-range-scan/summary.md) is a rejected ordered-subtree scan experiment and stale/rejected evidence for the current workload scope, and [`mutation-invalidation-breadth-index/summary.md`](mutation-invalidation-breadth-index/summary.md) is rejected/slice-context current-shape evidence: `invalidations.scanned_entries` dropped (`15392 -> 572` set, `10764 -> 52` focused) but the latency gate failed (`symlink_parent_mkdir_rmdir` p50 `1.239281x`, `pinned_symlink_parent_mkdir_rmdir` p50 `1.295362x`, set `subtree_rename_cached_unrelated` p50 `1.439480x`, focused subtree p50 `1.027902x`), so the child-index Rust implementation was reverted/not kept. |
| `open_confined` / `openat2` frequency | `smoke` | regenerated [`current-open-confined-surface-smoke.md`](current-open-confined-surface-smoke.md), regenerated [`current-metadata-opendir-smoke.md`](current-metadata-opendir-smoke.md), context-only [`metadata-open-path-claim/guard-context-worktree-19ca971/summary.md`](metadata-open-path-claim/guard-context-worktree-19ca971/summary.md) | current mixed and focused smokes exercise `open_confined_openat2`, `open_like.pre_open_guard.*`, `open_like.post_open_revalidation.*`, `source_root_path`, `resolved_virtual_path_from_open_fd`, `fuse_op.open`, and `fuse_op.opendir`; the focused opendir smoke keeps `readdir*` counters at zero and shows the row can be isolated; the regenerated smokes therefore provide coverage for the surrounding-cost split around the open-confined lane, and the broader metadata/open-path guard-context bundle shows these counts can move in the expected direction | an independent `open_confined` claim-grade speedup; proof that `openat2` itself, rather than surrounding policy/path work, is the dominant remaining cost; proof that the `open_like.pre_open_guard.*` / `open_like.post_open_revalidation.*` splits are speedup evidence by themselves; a kept fast-path baseline; or that a focused `metadata_opendir` improvement can override a failed `open-confined-surface` gate | treat this as the current next measure-first candidate and keep the same surface matrix for any future attempt: `--policy-preset fallback-unsafe-policy --workload-set open-confined-surface`, `--policy-preset fast-path-cache-eligible --workload-set open-confined-surface`, and explicit `--workload metadata_opendir` only as opendir-specific attribution/non-regression context | [`open-confined-frequency/summary.md`](open-confined-frequency/summary.md) is rejected/slice-context evidence: the focused fallback `metadata_opendir` row improved (`0.658506x` p50) and reduced resolver/open-confined counters, but the final fallback `open-confined-surface` row regressed (`metadata_open` p50 `1.341903x`, `metadata_opendir` p50 `1.152927x`) and the fast row kept tail regressions, so the implementation was reverted/not kept. |
| matcher-heavy policy path | `smoke` | [`current-policy-heavy-matrix-smoke.md`](current-policy-heavy-matrix-smoke.md), [`current-matcher-descendant-directory-smoke.md`](current-matcher-descendant-directory-smoke.md), context-only [`metadata-open-path-claim/summary.md`](metadata-open-path-claim/summary.md) and [`metadata-open-path-claim/guard-context-worktree-19ca971/summary.md`](metadata-open-path-claim/guard-context-worktree-19ca971/summary.md) | current matcher32 smoke remains metadata-heavy context that proves subtree-family candidate volume and non-zero path/descendant candidate-order counters; dedicated matcher-descendant smoke adds non-empty visible descendant workload coverage for `matcher_descendant_readdir` / `matcher_descendant_readdirplus` and keeps non-zero `matcher_candidate_order.descendant`, `readdir_directory_scan`, `readdirplus_directory_scan`, `readdirplus_attr_generation_scan`, `readdirplus_candidate_selection`, and `readdirplus_page_commit` counters | a before/after matcher-descendant improvement; a claim-grade speedup on `matcher_descendant_readdir` / `matcher_descendant_readdirplus`; duplicate-removal cost; broader directory-hot or symlink-visibility claims; or any descendant claim that still relies only on empty visible-matcher counters or one-way smoke timing | claim-grade matcher32 policy row plus a same-surface before/after `--workload-set matcher-descendant-directory` pair for `matcher_descendant_readdir` / `matcher_descendant_readdirplus`, or focused trace/microbenchmark/counter evidence if aggregate counters stay ambiguous | [`matcher-descendant-combined-probe/summary.md`](matcher-descendant-combined-probe/summary.md) is rejected combined-probe evidence: `metadata_lookup` / `metadata_access` stayed near-neutral-to-regressive, `matcher_descendant_readdir` improved only marginally and missed p99 non-regression, `matcher_descendant_readdirplus` regressed, the state gate failed, and the Rust optimization was reverted/not kept. |

## Honest-scope reminders

- `readdirplus-deferred-attrs` is an attr-work reduction slice, not a blanket `readdirplus` speedup claim across directory sizes.
- [`readdirplus-20k-policy-ablation/summary.md`](readdirplus-20k-policy-ablation/summary.md) is current dirty-worktree coverage only: the companion provenance records `git_worktree_clean=False` / `screenfs_source_git_worktree_clean=False`, includes regenerated open-like attribution docs plus `scripts/bench-screenfs.py`, `src/fs.rs`, `src/fs/guards.rs`, `src/fs/perf.rs`, `src/fs/tests/perf.rs`, and separates 5k/20k plus fallback/fast policy effects better than the rejected path-join bundle. It is not a before/after speedup claim, not a kept `readdirplus` optimization claim, and not an isolated no-code-change control.
- `readdirplus-page-scan-path-join` is rejected evidence only: some focused 20k counters/tails improved, but the implementation was reverted after 5k `readdir_basic` regressed badly and 20k `readdirplus_basic` p50 still regressed.
- `read-write-small-io-guard-reuse` is a fallback-policy claim bundle kept here as baseline/context. [`read-write-followup-current/summary.md`](read-write-followup-current/summary.md), [`read-write-storage-backed/summary.md`](read-write-storage-backed/summary.md), and [`read-write-cold-cache-approx/summary.md`](read-write-cold-cache-approx/summary.md) now add current fast/fallback default rows, alternate same-`64MiB` reduced small/random mix rows, separate `256MiB` larger-sequential rows, local btrfs storage-backed/concurrency coverage, and local btrfs non-root read-side `posix-fadvise-read-fixture` approximation coverage. They are still current dirty-worktree local coverage only: not before/after claims, not separate fast-policy claims, and not strict literal cold-cache evidence.
- mutation invalidation currently has observability and focused correctness coverage, not a kept speedup artifact; the current lane is breadth-first, the previous ordered-range scan bundle is rejected/stale for the current workload scope, the newer breadth-index bundle is also rejected/slice-context only because scanned-entry wins did not clear the latency gate, and post-`FORGET` observability must be added separately or explicitly documented as limitation/rejected/coverage artifact.
- `open_confined` currently has metadata/open-path attribution context plus rejected `open-confined-frequency` slice evidence, not a kept independent claim-grade bundle; regenerated [`current-open-confined-surface-smoke.md`](current-open-confined-surface-smoke.md) and [`current-metadata-opendir-smoke.md`](current-metadata-opendir-smoke.md) add surrounding-cost split coverage via `open_like.pre_open_guard.*` / `open_like.post_open_revalidation.*`, but those counters and focused `metadata_opendir` improvements do not override failing `open-confined-surface` rows.
- matcher-heavy policy evidence now includes metadata-heavy context plus dedicated descendant smoke/coverage: [`current-policy-heavy-matrix-smoke.md`](current-policy-heavy-matrix-smoke.md) keeps the matcher32 metadata row, and [`current-matcher-descendant-directory-smoke.md`](current-matcher-descendant-directory-smoke.md) proves non-empty visible descendant workload coverage with non-zero descendant/directory/readdirplus counters. [`matcher-descendant-combined-probe/summary.md`](matcher-descendant-combined-probe/summary.md) adds rejected combined-probe evidence only: the state gate failed, the Rust optimization was reverted/not kept, and the kept value is harness coverage rather than a speedup claim.
